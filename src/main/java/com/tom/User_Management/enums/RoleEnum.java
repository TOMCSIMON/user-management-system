package com.tom.User_Management.enums;

public enum RoleEnum {
    ROLE_USER,
    ROLE_ADMIN
}

/*Enum ensures that only valid, predefined roles (ROLE_USER or ROLE_ADMIN) are used,
 preventing typos and keeping role assignments type-safe throughout the code.*/
